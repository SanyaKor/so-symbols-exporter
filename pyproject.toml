[project]
name = "so-symbols-exporter"
version = "0.1.0"
description = "List native functions defined in a Linux shared object (.so)"
requires-python = ">=3.13"
dependencies = []


[dependency-groups]
dev = ["pytest>=9.0"]
